spring:
  cloud:
    config:
      override-system-properties: false
management:
  endpoints:
    web:
      exposure:
        include: info, health

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/